import { ElementRef } from '@angular/core';
import { FlashMessageService } from './flash-message.service';
export declare class FlashMessagesComponent {
    private flashMsgService;
    private el;
    private messagePositioning;
    private flashMsg;
    private readonly;
    constructor(flashMsgService: FlashMessageService, el: ElementRef);
    private show(flashMsg);
    private destroy(uuid);
}
